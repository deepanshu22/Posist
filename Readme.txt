Steps to compile it:
1)Run the file
2)Perform the actions prompted

The Details of the project are as follows-:
1) Creation of a tree(With necessary parameters)
2) Creating an insert function
3)Before the insertion of the data, we have to hash it and save the key in a global array
4) Unique node Id is given by global variable that is incremented every time an insertion is made in the tree.
5)edit performed by checking the node to be edited and then replacing the desired values.  